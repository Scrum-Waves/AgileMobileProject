import 'package:flutter/material.dart';

class AppColorsDark {
  static const primarySwatchColor = Colors.blueGrey;
  static const primaryColor = Colors.deepPurpleAccent;
  static const appBarColor = Colors.deepPurple;
  static const floatingActionButtonColor = Colors.deepPurple;
  static const floatingIconColor = Colors.white;
  //static const iconColor = Color.fromARGB(255, 44, 44, 42);
  static const iconColor = Color.fromARGB(255, 227, 227, 227);
  static const tileIconColor = Colors.deepPurpleAccent;
  //
  static const dialogBgColor = Color.fromARGB(255, 44, 44, 42);
  //static const scaffoldBgColor = Colors.white;
  //static const scaffoldBgColor = Color(0xff343541);
  static const scaffoldBgColor = Color.fromARGB(255, 44, 44, 42);
  /*static const textButtonColor = Colors.red;*/
  //shadow for button
  static const boxShadowColor = Color.fromRGBO(255, 255, 255, 0.57);
  //Divider Color
  static const dividerColor = Colors.grey;

  /////////////////////////////////////////////////Extra
}
